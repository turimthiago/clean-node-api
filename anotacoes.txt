jest --clearCache

video 42 [X]

Assuntos:
Hash = critografa uma string (123 sempre terá o mesmo hash), é irreversível;
Encrypter = criptografa uma string utlizando uma chave secreta, é reversível;
Encoding = embaralha uma string para transporte (ex. url com query params);